#include "main.h"

/**
  * is_palindrome - ksjdfk
  * @s: klsjdf
  * Return: lksjdf
  */

int is_palindrome(char *s)
{
	char *revs;

	revs = strrev(s);
	if (revs == s)
	{
		return (1);
	}
	return (0);
}
