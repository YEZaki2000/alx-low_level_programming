#include "main.h"

/**
 * *_strcat -  concatenates two strings.
 * @dest: ldjskf
 * @src: lfksdjf
 * return: char
 */

char *_strcat(char *dest, char *src)
{
	dest = strcat(dest, src);
	return(dest);
}
