#include "main.h"

/**
* isupper - a function that checks for
* lowercase character.
* @c: character to be checked.
*
* Return: 0 or 1.
*/

int _isupper(int c)
{
	if (isupper(c) != 0)
	{
		return (1);
	}
	return (0);
}
